# PRD: 무한 뽁뽁이 (Bubble Wrap)

## 1. 배경

토스 앱인토스(Apps in Toss) 8월 바이브코딩 챌린지 "하찮아도 괜찮아" 출품작.
- 주제: 작고 귀엽고 웃음이 나오는, "이런 걸 누가 쓰겠어" 싶지만 자꾸 손이 가는 미니앱
- 출품 기간: 2026-08-03 ~ 2026-08-26
- 심사: 1차는 지표(사용성/재방문 등) 종합, 최종은 테마 적합성 + UX 종합 평가. 단순 리워드성 앱, 주제 이탈작은 제외
- 참고: [8월 바이브코딩 챌린지 공고](https://toss.im/apps-in-toss/blog/2608_vibecoding_challenge)

## 2. 한 줄 요약

에어캡 뽁뽁이를 무한히 눌러 터뜨리는, 그 이상도 이하도 아닌 앱. 다만 계속 누르고 싶어지는 하찮은 장치들을 붙였다.

## 3. 타깃 사용자

- 스트레스 풀 거리를 찾는 사람
- 심심할 때 잠깐 켜서 몇 번 누르고 끄는 라이트 유저
- "귀엽고 하찮은 것"에 반응하는 챌린지 심사자/공유 유저

## 4. 목표

- **주제 적합성**: 실용성 0에 수렴하되, 누르는 행위 자체의 손맛과 리액션으로 웃음을 유발
- **재방문 유도**: 누적 기록(칭호, 총 팝 개수)이 로컬에 남아 "이어서 하고 싶은" 이유를 만듦
- **완결성**: 짧은 세션 안에서도 시작-진행-완료(시트 클리어)-보상(칭호/골든/통계)의 루프가 끊기지 않게 함

### Non-goals
- 리워드/포인트 지급, 광고 연동 등 리워드성 장치 (심사 제외 사유이므로 의도적으로 배제)
- 멀티플레이, 랭킹/소셜 공유 기능 (v1 범위 밖)
- 서버/백엔드 — 모든 상태는 `localStorage` 기반 클라이언트 전용

## 5. 핵심 경험 (Core Loop)

1. 6×14(84칸) 뽁뽁이 시트가 화면에 뜬다
2. 버블을 누르면 `pop` 사운드 + 오그라드는 애니메이션과 함께 터진다 (터진 버블은 되돌릴 수 없음)
3. 84칸을 모두 터뜨리면 "새 뽁뽁이 한 장 더" 버튼이 나타나고, 누르면 새 시트로 리필된다
4. 팝을 반복할수록 아래 장치들이 우연히/필연적으로 발동하며 루프에 변주를 준다

## 6. 기능 명세

### 6.1 기본 팝 메커닉
- 6열 × 14행 = 84개 버블/시트
- 버블당 1회만 팝 가능(`disabled` 처리), 팝 시 축소 애니메이션
- 진행률 `N / 84 팡` 상시 노출
- `sound.ts`: Web Audio API로 팝(하강 사인파), 리필(2음 트라이앵글) 효과음 생성 — 별도 오디오 에셋 없음

### 6.2 칭호(등급) 시스템
- 누적 팝 개수(`total`) 기준 9단계 칭호 사다리 (`src/titles.ts`)

  | 누적 개수 | 칭호 |
  |---|---|
  | 0 | 뽁뽁이 새싹 |
  | 10 | 뽁뽁이 견습생 |
  | 50 | 뽁뽁이 중독자 |
  | 100 | 뽁뽁이 장인 |
  | 300 | 뽁뽁이 도인 |
  | 600 | 뽁뽁이의 신 |
  | 1,000 | 손가락에 굳은살 박힌 자 |
  | 3,000 | 이 정도면 직업병 |
  | 10,000 | 전설의 뽁뽁이 |

- 헤더에 현재 칭호 + "다음 칭호까지 N개" 상시 표시
- 칭호가 바뀌는 순간 팡파레 사운드 + 토스트("🎉 승급! 이제 당신은 '…' 입니다")

### 6.3 황금 뽁뽁이
- 새 시트가 생성될 때(최초 로드 포함) 25% 확률로 시트 내 임의의 한 칸이 "황금 뽁뽁이"로 지정됨
- 시각적으로 금색 그라디언트 + 은은한 pulse 애니메이션으로 구분되어 눈에 띔
- 터뜨리면: 전용 사운드, confetti 이펙트, 랜덤 축하 멘트 토스트(`src/messages.ts`의 `GOLDEN_MESSAGES`), 팝 보너스 +9개, 누적 발견 개수(`goldenTotal`) 별도 저장

### 6.4 콤보 리액션
- 0.9초 이내 연속 팝을 콤보로 카운트(타임아웃 시 리셋)
- 콤보 5 / 10 / 20 / 35 / 50 임계값에서 화면 중앙에 리액션 텍스트가 튀어나왔다 사라짐(`src/messages.ts`의 `COMBO_MESSAGES`, 예: "우다다다!!", "이성을 잃었다")

### 6.5 쓸모없는 통계
- 하단 "🧮 쓸모없는 통계 보기" 버튼으로 모달 오픈(`src/StatsModal.tsx`)
- 누적 개수를 근거 없는 계수로 환산한 완전히 무의미한 지표들을 보여줌:
  - 다 채운 시트 수
  - 일렬로 늘어놓은 길이(m) → 버스 대수 환산
  - 추정 소요 시간(분) → 라면 끓이는 시간 환산
  - 추정 소모 칼로리(kcal)
  - 황금 뽁뽁이 발견 개수

## 7. 데이터 & 상태

- 서버 없음. 전부 클라이언트 `localStorage`에 저장
  - `bubble-wrap-total-popped`: 누적 총 팝 개수 (칭호 산정 기준, 골든 보너스 포함)
  - `bubble-wrap-golden-popped`: 누적 황금 뽁뽁이 발견 개수
- 현재 진행 중인 시트(어떤 칸이 터졌는지, 골든 위치)는 새로고침 시 초기화됨 — **의도된 동작은 아니며 향후 개선 후보** (§9 참고)

## 8. 기술 스택

- React 19 + TypeScript, Vite 8 (Rolldown 기반)
- `@apps-in-toss/web-framework` — 토스 앱인토스 웹 미니앱 프레임워크
- `babel-plugin-react-compiler` — 자동 메모이제이션, `oxlint`의 `react(purity)` 규칙으로 렌더 순수성 검증
- 사운드: Web Audio API 오실레이터 직접 합성 (외부 에셋 없음)
- 배포: `ait build` / `ait deploy` (apps-in-toss devtools)

## 9. 열린 질문 / 향후 후보

- 현재 진행 중인 시트 상태(팝 여부, 골든 위치)를 `localStorage`에 영속화할지 — 지금은 새로고침하면 새 시트로 시작
- 황금 뽁뽁이 등장 확률(25%)·보너스(+9)·콤보 임계값 등 밸런스 수치는 실사용 반응을 보며 튜닝 필요
- 공유하기(스크린샷/칭호 카드) 등 바이럴 장치는 v1 범위 밖이나, 테마 적합성 강화를 위해 검토 가능
- 접근성: 스크린리더 대상 아리아 라벨은 있으나, 다크모드/고대비 등은 미검증
